(function (window) {
  window.__env = window.__env || {};

  // API Configuration
  window.__env.FINERACT_API_URL = "${FINERACT_API_URL}";
  window.__env.FINERACT_API_PROVIDER = "${FINERACT_API_PROVIDER}";
  window.__env.FINERACT_API_VERSION = "${FINERACT_API_VERSION}";
  window.__env.FINERACT_PLATFORM_TENANT_IDENTIFIER =
    "${FINERACT_PLATFORM_TENANT_IDENTIFIER}";

  // Language & Locale
  window.__env.MIFOS_DEFAULT_LANGUAGE = "${MIFOS_DEFAULT_LANGUAGE}";
  window.__env.MIFOS_SUPPORTED_LANGUAGES = "${MIFOS_SUPPORTED_LANGUAGES}";

  // Feature Flags
  window.__env.MIFOS_PRELOAD_CLIENTS = "${MIFOS_PRELOAD_CLIENTS}";
  window.__env.MIFOS_DEFAULT_CHAR_DELIMITER =
    "${MIFOS_DEFAULT_CHAR_DELIMITER}";

  // OAuth / OIDC
  window.__env.MIFOS_OAUTH_SERVER_ENABLED = "${MIFOS_OAUTH_SERVER_ENABLED}";
  window.__env.FINERACT_PLUGIN_OIDC_ENABLED =
    "${FINERACT_PLUGIN_OIDC_ENABLED}";
  window.__env.FINERACT_PLUGIN_OIDC_BASE_URL =
    "${FINERACT_PLUGIN_OIDC_BASE_URL}";
  window.__env.FINERACT_PLUGIN_OIDC_CLIENT_ID =
    "${FINERACT_PLUGIN_OIDC_CLIENT_ID}";
  window.__env.FINERACT_PLUGIN_OIDC_API_URL =
    "${FINERACT_PLUGIN_OIDC_API_URL}";
  window.__env.FINERACT_PLUGIN_OIDC_FRONTEND_URL =
    "${FINERACT_PLUGIN_OIDC_FRONTEND_URL}";
})(this);
